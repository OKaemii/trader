apiVersion: v1
kind: Service
metadata:
  name: backtest-engine
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  selector:
    app: backtest-engine
  ports:
    - name: http
      port: 8001
      targetPort: 8001
      protocol: TCP
