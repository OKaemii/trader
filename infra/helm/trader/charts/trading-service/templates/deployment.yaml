apiVersion: apps/v1
kind: Deployment
metadata:
  name: trading-service
  namespace: {{ .Release.Namespace }}
spec:
  replicas: {{ .Values.replicaCount | default 1 }}
  selector:
    matchLabels:
      app: trading-service
  template:
    metadata:
      labels:
        app: trading-service
    spec:
      containers:
        - name: trading-service
          image: {{ .Values.image | default "trader/trading-service:latest" }}
          imagePullPolicy: Never
          ports:
            - containerPort: 3005
          envFrom:
            - secretRef:
                name: trader-secrets
          env:
            - name: LOG_LEVELS
              value: {{ .Values.global.env.logLevels | default "error,warn" | quote }}
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: trader-secrets
                  key: REDIS_PASSWORD
            - name: MONGODB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: trader-secrets
                  key: MONGODB_PASSWORD
            - name: REDIS_URL
              value: "redis://:$(REDIS_PASSWORD)@redis-master:6379"
            - name: MONGODB_URL
              value: "mongodb://trader:$(MONGODB_PASSWORD)@mongodb-headless:27017/trader?authSource=trader&directConnection=true"
            - name: TIMESCALEDB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: trader-secrets
                  key: TIMESCALEDB_PASSWORD
            - name: TIMESCALE_URL
              value: "postgresql://trader:$(TIMESCALEDB_PASSWORD)@timescaledb-postgresql:5432/trader_ts"
            - name: BARS_BACKEND
              value: {{ .Values.global.env.barsBackend | default "mongo" | quote }}
            - name: TRADING_MODE
              value: {{ .Values.global.env.tradingMode | default "Paper" | quote }}
            - name: ACTIVE_STRATEGY
              value: {{ .Values.global.env.activeStrategy | default "factor_rank_v1" | quote }}
            - name: SIGNAL_ORDER_TYPE
              value: {{ .Values.global.env.signalOrderType | default "Limit" | quote }}
            - name: FILL_POLL_INTERVAL_MS
              value: {{ .Values.global.env.fillPollIntervalMs | default "30000" | quote }}
            - name: ORDER_MIN_INTERVAL_MS
              value: {{ .Values.global.env.orderMinIntervalMs | default "1100" | quote }}
            - name: ACCOUNT_CACHE_TTL_MS
              value: {{ .Values.global.env.accountCacheTtlMs | default "30000" | quote }}
            - name: ORDER_MAX_ATTEMPTS
              value: {{ .Values.global.env.orderMaxAttempts | default "5" | quote }}
            - name: QUEUE_TTL_MS
              value: {{ .Values.global.env.queueTtlMs | default "3600000" | quote }}
            - name: PRICE_DRIFT_TOLERANCE
              value: {{ .Values.global.env.priceDriftTolerance | default "0.01" | quote }}
            - name: SIGNAL_SERVICE_URL
              value: "http://signal-service:3003"
          livenessProbe:
            httpGet:
              path: /health
              port: 3005
            initialDelaySeconds: 15
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 3005
            initialDelaySeconds: 10
            periodSeconds: 10
