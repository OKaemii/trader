apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: signal-service
  namespace: {{ .Release.Namespace }}
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: signal-service
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
