{
  "name": "@trader/shared-types",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc -b --noEmit",
    "build": "tsc -b",
    "lint": "eslint src",
    "test": "vitest run"
  }
}
