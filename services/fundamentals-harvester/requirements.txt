# fundamentals-harvester — the PIT fundamentals LAKE write path (epic Task 8).
#
# The on-disk Parquet SCHEMA, the next-session knowledge_ts calendar, and the pyarrow/duckdb
# scientific pins come from the quant-core package's [lake] extra, installed FIRST from
# packages/quant-core (see the harvester Dockerfile, a LATER card). quant-core is the single source of
# truth for those pins — do NOT re-pin pyarrow here. Installing the [lake] extra (pyarrow>=16,<18 +
# duckdb) gives the harvester both `quant_core.fundamentals.lake.schema.SCHEMA` and
# `...lake.calendar.derive_knowledge_ts`, the exact modules the DuckDB read engine reads, so writer
# and reader cannot drift.
#
#   pip install './packages/quant-core[lake]'
#
# Listed below is only the harvester's own direct upstream dependency that quant-core does not pull:

# SEC EDGAR async HTTP client (companyfacts / submissions / daily form index / bulk-zip download).
# Same pin as quant-core's [http] extra and the other EDGAR callers, to avoid a split.
httpx==0.27.2

# Thin FastAPI status surface (src/app.py) — the portal's read-only window into the lake (status / config /
# freshness / runs + force-sweep). Same pins as the read-side services (fundamentals-api / backtest-engine),
# to avoid a split; starlette (FastAPI's ASGI core, which backs TestClient + JSONResponse) is pulled
# transitively by this fastapi pin, matching how those services declare it. uvicorn runs the app's HTTP
# server in the deployed pod; the status surface shares the lake RW mount with the harvest loop — wired by
# the chart (a later card). The PR gate already has fastapi via backtest-engine's requirements + httpx via
# quant-core[http], so the TestClient suite needs no extra gate install.
fastapi==0.111.0
uvicorn==0.30.1

# Test-only
pytest==8.3.3
pytest-asyncio==0.24.0
