# Scientific stack (numpy/scipy/scikit-learn/giotto-tda/cvxpy) comes from the quant-core
# package, installed from packages/quant-core (see backtest-engine.Dockerfile). quant-core is
# the single source of truth for those pins — do NOT re-pin them here. (The old
# scikit-learn==1.5.0 conflicted with quant-core's 1.3.2, required for giotto-tda 0.6.2.)
fastapi==0.111.0
uvicorn==0.30.1
motor==3.5.1
pymongo==4.7.0
redis==5.0.4
shap==0.45.1
pandas==2.2.2
duckdb==1.1.3

# Test-only
pytest==8.3.3
pytest-asyncio==0.24.0
