{
  "name": "@trader/frontend-web",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "cd portal && pnpm install && pnpm dev -- -p 3007",
    "build": "echo 'frontend-web build is delegated to portal/ (run pnpm --filter portal build directly)'",
    "start": "cd portal && pnpm start -- -p 3007",
    "typecheck": "echo 'frontend-web typecheck is delegated to portal/ (Next.js owns its own toolchain)'"
  },
  "dependencies": {
    "@trader/shared-types": "workspace:*",
    "next": "^14.2.0",
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "recharts": "^2.12.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.0",
    "tailwindcss": "^3.4.0",
    "typescript": "^5.4.0"
  }
}
