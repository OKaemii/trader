# The scientific stack (numpy/scipy/scikit-learn/giotto-tda/cvxpy) and ALL strategy code now
# come from the quant-core package, installed from packages/quant-core (see
# strategy-engine.Dockerfile, which `pip install`s it before this file). quant-core is the
# single source of truth for those pins — do NOT re-pin them here or the two can drift.
# Local dev / CI: `pip install -e packages/quant-core` before `pip install -r` this file.
fastapi==0.115.0
uvicorn[standard]==0.30.0
redis[hiredis]==5.0.8
prometheus-client==0.20.0
motor==3.5.1
pymongo==4.7.0
httpx==0.27.2

# Test-only — kept in the same requirements file for simplicity. Split into a
# requirements-dev.txt + multi-stage Dockerfile if image size becomes a concern.
pytest==8.3.3
pytest-asyncio==0.24.0
respx==0.21.1
