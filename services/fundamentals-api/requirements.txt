# fundamentals-api — the READ-side of the PIT Fundamentals LAKE (epic Task 10).
#
# The scientific stack, the fundamentals contract (quant_core.fundamentals: LINE_ITEMS / market_of /
# SOURCE_*), AND the lake read engine (quant_core.fundamentals.lake: store/metrics/contract — the
# DuckDB-over-Parquet PIT reader) come from the quant-core package, installed with the [lake] extra
# (duckdb + pyarrow) FIRST from packages/quant-core (see infra/docker/fundamentals-api.Dockerfile).
# quant-core is the single source of truth for those pins — do NOT re-pin them here. The service no
# longer holds a Timescale connection (the lake replaced the bi-temporal hypertable), so asyncpg /
# motor / pymongo are GONE from this list.
fastapi==0.111.0
uvicorn==0.30.1

# Redis read-through cache fronting the as-of resolver + the shared FX-key reader the Gap-2 enrichment
# uses (market_cap.py). redis-py's async client; the same major the platform uses.
redis==5.0.7

# Prometheus /metrics exposition — the read-side ServiceMonitor scrapes it (request-latency histogram →
# API p50/p95). Same pin as strategy-engine.
prometheus-client==0.20.0

# Test-only
pytest==8.3.3
pytest-asyncio==0.24.0
